# ALU Connect — Design Refresh

This is a redesigned copy of the app with **visible UI changes** focused on:

1. **Onboarding / login screen** — glass card hero, gradient background, campus badges, redesigned social buttons
2. **Profile setup sheet** — amber top accent, section cards, custom campus/role pickers, animated mission chips

## Run this version

```bash
cd ~/Documents/alu_connect_design
flutter pub get
flutter run
```

## Original project

The original copy remains at `~/Downloads/alu_connect`.
